const defaults = {
    // default query string
    limit : 10,
    page  : 1,
    search : '',
    sortBy : 'updatedAt',
    sortType : 'desc',
    realation : null

}

export default Object.freeze(defaults)